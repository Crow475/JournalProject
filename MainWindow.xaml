<Window x:Class="JournalProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JournalProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280" Loaded="Window_Loaded">
    <Grid Background="White">
        <TabControl >
            <TabItem Header="Articles">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="58*"/>
                        <ColumnDefinition Width="15*"/>
                        <ColumnDefinition Width="691*"/>
                        <ColumnDefinition Width="510*"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel Grid.ColumnSpan="3">
                        <DockPanel DockPanel.Dock="Top" Margin="0,2,0,2">
                            <Button x:Name="ArticleRefreshButton" Content="Refresh" DockPanel.Dock="Left" Margin="0,0,5,0" FontWeight="DemiBold" Padding="3,1,3,1" Click="ArticleRefreshButton_Click"/>
                            <Button x:Name="ArticleSearchButton" Content="Search" DockPanel.Dock="Right" Margin="5,0,0,0" FontWeight="DemiBold" Padding="3,1,3,1" Click="ArticleSearchButton_Click"/>
                            <TextBox x:Name="ArticleSearchTextbox" AcceptsReturn="False" HorizontalAlignment="Stretch" Padding="3,2,3,2"/>
                        </DockPanel>
                        <DataGrid x:Name="AllArticlesDataGrid" AutoGenerateColumns="True" SelectedCellsChanged="ArticleSelected" SelectionUnit="FullRow" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False" VerticalGridLinesBrush="#FFF1F1F1" Background="White"/>
                    </DockPanel>
                    <Grid Grid.Column="3" x:Name="CreateNewArticlePanel">
                        <Button x:Name="CreateNewArticleButton" Width="150" Height="40" FontWeight="Bold" Click="CreateNewArticleButton_Click" FontSize="15">
                            Create new article
                        </Button>
                    </Grid>
                    <DockPanel x:Name="EditArticlePanel" Grid.Column="3" Visibility="Collapsed" >
                        <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                            <WrapPanel.Resources>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Padding" Value="5" />
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </WrapPanel.Resources>
                            <Button x:Name="ArticleCreateButton" Click="ArticleCreateButton_Click" Background="#FFD9F99D" FontWeight="DemiBold">
                                Add new
                            </Button>
                            <Button x:Name="ArticleEditButton" Click="ArticleEditButton_Click" Background="#FFfef08a" FontWeight="DemiBold">
                                Confirm edit
                            </Button>
                            <Button x:Name="ArticleDeleteButton" Click="ArticleDeleteButton_Click" Background="#FFf87171" FontWeight="DemiBold">
                                Delete
                            </Button>
                            <Button x:Name="ArticleCancelButton" Click="ArticleCancelButton_Click" FontWeight="DemiBold">
                                Cancel
                            </Button>
                        </WrapPanel>
                        <Border Padding="10">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="Label">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </StackPanel.Resources>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Title:
                                    </Label>
                                    <TextBox x:Name="ArticleTitleTextBox" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        Journal:
                                    </Label>
                                    <ComboBox x:Name="JournalsCombobox" Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2">

                                    </ComboBox>
                                    <Label Grid.Row="2">
                                        Pages:
                                    </Label>
                                    <Grid Grid.Row="2" Grid.Column="1" Margin="5, 5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>
                                        <DockPanel Grid.Column="0">
                                            <Label DockPanel.Dock="Left">
                                                From:
                                            </Label>
                                            <TextBox x:Name="ArticlePageStart" Margin="0, 0, 10, 0" Padding="3,2,3,2"/>
                                        </DockPanel>
                                        <DockPanel Grid.Column="1">
                                            <Label DockPanel.Dock="left">
                                                To:
                                            </Label>
                                            <TextBox x:Name="ArticlePageEnd" Padding="3,2,3,2"/>
                                        </DockPanel>
                                    </Grid>
                                </Grid>
                                <Label>
                                    Authors:
                                </Label>
                                <DataGrid x:Name="articleAuthors" AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="False" Height="400" GridLinesVisibility="None" CanUserAddRows="False" Background="White">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="First Name" Binding="{Binding first_name}" Width="200"/>
                                        <DataGridTextColumn Header="Last Name" Binding="{Binding last_name}" Width="200"/>
                                        <DataGridTemplateColumn>
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Click="Button_Click" >
                                                        Remove
                                                    </Button>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="AuthorsCombobox" IsEditable="True" IsTextSearchEnabled="True" Margin="0,5,0,0" Padding="3,2,3,2" TextInput="AuthorsCombobox_TextInput">
                                    </ComboBox>
                                    <Button x:Name="AuthorAddButton" Grid.Column="1" Margin="10,5,0,0" Click="AuthorAddButton_Click">
                                        Add
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Authors">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="33*"/>
                        <ColumnDefinition Width="731*"/>
                        <ColumnDefinition Width="510*"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel Grid.ColumnSpan="2">
                        <DockPanel DockPanel.Dock="Top" Margin="0,2,0,2">
                            <Button x:Name="AuthorRefreshButton" Content="Refresh" DockPanel.Dock="Left" Margin="0,0,5,0" Padding="3,1,3,1" FontWeight="DemiBold" Click="AuthorRefreshButton_Click"/>
                            <Button x:Name="AuthorSearchButton" Content="Search" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="3,1,3,1" FontWeight="DemiBold" Click="AuthorSearchButton_Click"/>
                            <TextBox x:Name="AuthorSearchTextbox" AcceptsReturn="False" HorizontalAlignment="Stretch" Padding="3,2,3,2"/>
                        </DockPanel>
                        <DataGrid x:Name="allAuthorsDatagrid" AutoGenerateColumns="True" SelectedCellsChanged="dataGrid2_SelectedCellsChanged" SelectionUnit="FullRow" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False" VerticalGridLinesBrush="#FFF1F1F1" Background="White"/>
                    </DockPanel>
                    <Grid Grid.Column="1" x:Name="CreateNewAuthorPanel" Grid.ColumnSpan="2" Margin="731,0,0,0">
                        <Button x:Name="CreateNewAuthorButton" Width="150" Height="40" FontWeight="Bold" Click="CreateNewAuthorButton_Click" FontSize="15">
                            Add new author
                        </Button>
                    </Grid>
                    <DockPanel x:Name="EditAuthorPanel" Grid.Column="1" Visibility="Collapsed" Grid.ColumnSpan="2" Margin="731,0,0,0">
                        <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                            <WrapPanel.Resources>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Padding" Value="5" />
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </WrapPanel.Resources>
                            <Button x:Name="AuthorCreateButton" Click="AuthorCreateButton_Click" Background="#FFD9F99D" FontWeight="DemiBold">
                                Add new
                            </Button>
                            <Button x:Name="AuthorEditButton" Click="AuthorEditButton_Click" Background="#FFfef08a" FontWeight="DemiBold">
                                Confirm edit
                            </Button>
                            <Button x:Name="AuthorDeleteButton" Click="AuthorDeleteButton_Click" Background="#FFf87171" FontWeight="DemiBold">
                                Delete
                            </Button>
                            <Button x:Name="AuthorCancelButton" Click="AuthorCancelButton_Click" FontWeight="DemiBold">
                                Cancel
                            </Button>
                        </WrapPanel>
                        <Border Padding="10">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="Label">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </StackPanel.Resources>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Last name:
                                    </Label>
                                    <TextBox x:Name="authorLastNameTextbox" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        First name:
                                    </Label>
                                    <TextBox x:Name="authorFirstNameTextbox" Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2" />
                                </Grid>
                                <Label>
                                    Articles:
                                </Label>
                                <DataGrid x:Name="authorsArticles" AutoGenerateColumns="True"  IsReadOnly="True" CanUserSortColumns="False" Height="400" GridLinesVisibility="None" CanUserAddRows="False" Background="White"/>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Journals">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <DataGrid x:Name="journalDataGrid" AutoGenerateColumns="False" SelectionUnit="FullRow" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False" SelectedCellsChanged="journalDataGrid_SelectedCellsChanged" VerticalGridLinesBrush="#FFF1F1F1" Background="White"/>
                        <Grid Grid.Row="1" x:Name="CreateNewJournalPanel" >
                            <Button Width="150" Height="40" FontWeight="Bold" x:Name="CreateNewJournalButton" Click="CreateNewJournalButton_Click" FontSize="15">Add new journal</Button>
                        </Grid>
                        <DockPanel x:Name="EditJournalPanel" Visibility="Collapsed" Grid.Row="1">
                            <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                                <WrapPanel.Resources>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Margin" Value="10,0" />
                                        <Setter Property="Padding" Value="5" />
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Style.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Style.Resources>
                                    </Style>
                                </WrapPanel.Resources>
                                <Button x:Name="JournalCreateButton" Click="JournalCreateButton_Click" Background="#FFD9F99D" FontWeight="DemiBold">
                                    Add new
                                </Button>
                                <Button x:Name="JournalEditButton" Click="JournalEditButton_Click" Background="#FFfef08a" FontWeight="DemiBold">
                                    Confirm edit
                                </Button>
                                <Button x:Name="JournalDeleteButton" Click="JournalDeleteButton_Click" Background="#FFf87171" FontWeight="DemiBold">
                                    Delete
                                </Button>
                                <Button x:Name="JournalCancelButton" Click="JournalCancelButton_Click" FontWeight="DemiBold">
                                    Cancel
                                </Button>
                            </WrapPanel>
                            <Border Padding="10">
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="Label">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Number:
                                    </Label>
                                    <TextBox x:Name="journalNumberTextBox" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        Year:
                                    </Label>
                                    <TextBox x:Name="publicationYearTextBox" Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2" />
                                </Grid>
                            </Border>
                        </DockPanel>
                    </Grid>
                    <DockPanel Grid.Column="1">
                        <DockPanel DockPanel.Dock="Bottom" Margin="0, 5">
                            <Button x:Name="JournalRefreshButton" Content="Refresh" Margin="0,0,5,0" Padding="4,2,4,2" DockPanel.Dock="Right" Click="JournalRefreshButton_Click" FontWeight="DemiBold"/>
                            <Border></Border>
                        </DockPanel>
                        <DataGrid x:Name="issueDataGrid" Grid.Column="1" AutoGenerateColumns="False" SelectionUnit="FullRow" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False" CanUserSortColumns="False" VerticalGridLinesBrush="#FFF1F1F1" Background="White"/>
                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
