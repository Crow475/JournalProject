<Window x:Class="JournalProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JournalProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280" Loaded="Window_Loaded">
    <Grid Background="LightGray">
        <TabControl >
            <TabItem Header="Articles">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Top" Margin="0,2,0,2">
                            <Button x:Name="ArticleSearchButton" Content="Search" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="3,1,3,1"/>
                            <TextBox AcceptsReturn="False" HorizontalAlignment="Stretch" DockPanel.Dock="Left" Padding="3,2,3,2"/>
                        </DockPanel>
                        <DataGrid x:Name="AllArticlesDataGrid" AutoGenerateColumns="True" SelectedCellsChanged="ArticleSelected" SelectionUnit="FullRow" IsReadOnly="True" SelectionMode="Single"/>
                    </DockPanel>
                    <Grid Grid.Column="1" x:Name="CreateNewArticlePanel">
                        <Button x:Name="CreateNewArticleButton" Width="150" Height="40" FontWeight="Bold" Click="CreateNewArticleButton_Click">
                            Create new article
                        </Button>
                    </Grid>
                    <DockPanel x:Name="EditArticlePanel" Grid.Column="1"  >
                        <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                            <WrapPanel.Resources>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Padding" Value="5" />
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </WrapPanel.Resources>
                            <Button x:Name="ArticleCreateButton" Click="ArticleCreateButton_Click">
                                Add new
                            </Button>
                            <Button x:Name="ArticleEditButton" Click="ArticleEditButton_Click">
                                Confirm edit
                            </Button>
                            <Button x:Name="ArticleDeleteButton" Click="ArticleDeleteButton_Click">
                                Delete
                            </Button>
                            <Button x:Name="ArticleCancelButton" Click="ArticleCancelButton_Click">
                                Cancel
                            </Button>
                        </WrapPanel>
                        <Border Padding="10">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="Label">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </StackPanel.Resources>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Title:
                                    </Label>
                                    <TextBox x:Name="ArticleTitleTextBox" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        Journal:
                                    </Label>
                                    <ComboBox x:Name="JournalsCombobox" Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2">
                                        
                                    </ComboBox>
                                    <Label Grid.Row="2">
                                        Pages:
                                    </Label>
                                    <Grid Grid.Row="2" Grid.Column="1" Margin="5, 5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>
                                        <DockPanel Grid.Column="0">
                                            <Label DockPanel.Dock="Left">
                                                From:
                                            </Label>
                                            <TextBox x:Name="ArticlePageStart" Margin="0, 0, 10, 0" Padding="3,2,3,2"/>
                                        </DockPanel>
                                        <DockPanel Grid.Column="1">
                                            <Label DockPanel.Dock="left">
                                                To:
                                            </Label>
                                            <TextBox x:Name="ArticlePageEnd" Padding="3,2,3,2"/>
                                        </DockPanel>
                                    </Grid>
                                </Grid>
                                <Label>
                                    Authors:
                                </Label>
                                <DataGrid x:Name="articleAuthors" AutoGenerateColumns="False" Height="400" GridLinesVisibility="None">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="First Name" Binding="{Binding first_name}" Width="200"/>
                                        <DataGridTextColumn Header="Last Name" Binding="{Binding last_name}" Width="200"/>
                                        <DataGridTemplateColumn>
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button>
                                                        Remove
                                                    </Button>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox x:Name="AuthorsCombobox" IsEditable="True" IsTextSearchEnabled="True" Margin="0,5,0,0" Padding="3,2,3,2" TextInput="AuthorsCombobox_TextInput">
                                    </ComboBox>
                                    <Button Grid.Column="1" Margin="10,5,0,0">
                                        Add
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Authors">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Top" Margin="0,2,0,2">
                            <Button x:Name="AuthorSearchButton" Content="Search" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="3,1,3,1"/>
                            <TextBox AcceptsReturn="False" HorizontalAlignment="Stretch" DockPanel.Dock="Left" Padding="3,2,3,2"/>
                        </DockPanel>
                        <DataGrid x:Name="dataGrid2" AutoGenerateColumns="True"/>
                    </DockPanel>
                    <DockPanel Grid.Column="1">
                        <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                            <WrapPanel.Resources>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Margin" Value="10,0" />
                                    <Setter Property="Padding" Value="5" />
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </WrapPanel.Resources>
                            <Button>
                                Add
                            </Button>
                            <Button>
                                Edit
                            </Button>
                            <Button>
                                Delete
                            </Button>
                        </WrapPanel>
                        <Border Padding="10">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="Label">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </StackPanel.Resources>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Last name:
                                    </Label>
                                    <TextBox Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        First name:
                                    </Label>
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2" />
                                </Grid>
                                <Label>
                                    Articles:
                                </Label>
                                <DataGrid x:Name="authorsArticles" AutoGenerateColumns="True" Height="400"/>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Journals">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <DataGrid x:Name="journalDataGrid" AutoGenerateColumns="True" />
                        <DockPanel Grid.Row="1">
                            <WrapPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0, 10">
                                <WrapPanel.Resources>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Margin" Value="10,0" />
                                        <Setter Property="Padding" Value="5" />
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Style.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Style.Resources>
                                    </Style>
                                </WrapPanel.Resources>
                                <Button>
                                    Add
                                </Button>
                                <Button>
                                    Edit
                                </Button>
                                <Button>
                                    Delete
                                </Button>
                            </WrapPanel>
                            <Border Padding="10">
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="Label">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label>
                                        Number:
                                    </Label>
                                        <TextBox Grid.Column="1" Margin="5, 5" Padding="3,2,3,2"/>
                                    <Label Grid.Row="1">
                                        Year:
                                    </Label>
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="5, 5" Padding="3,2,3,2" />
                            </Grid>
                            </Border>
                        </DockPanel>
                    </Grid>
                    <DataGrid x:Name="issueDataGrid" Grid.Column="1" AutoGenerateColumns="True"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
